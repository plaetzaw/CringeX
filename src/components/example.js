// // in the global state, have an initial state of:

// let initialState = { // handled with redux
//     bottomDisplay: true
// }

// // on Login Page
// /*
// 1. pull 'bottomDisplay' state from Global State as props
// 2. on Login Page load, change `bottomDisplay` state to `false`
// 3. if `bottomDisplay` is `false`, hide icons (handled with turnery statement)
// */

// // turnery statement for "if bottomDisplay is false"
// !bottomDisplay ? (
//     // what do you want it to display if TRUE
//     // remove className={classes.bottomIcon}
// ) : (
//     // what do you want it to display if FALSE
//     // add className={classes.bottomIcon} where `bottomIcon: { display: "none"}`
// )

// /*

// */
